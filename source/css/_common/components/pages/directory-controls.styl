// Share the dimensions of directory view switches, independent of content.
.category-directory, .tag-directory, .archive-page .archive-overview, .tag-detail-overview {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: start;
}
.category-directory > .item.title, .tag-directory > .item.title,
.archive-page .archive-heading, .tag-detail-heading {
  grid-column: 1;
  grid-row: 1;
  min-width: 0;
  padding-right: 0;
}
.tag-directory .tag-cloud-widget,
.archive-page .archive-toolbar,
.archive-page[data-mode='classic'] .archive-toolbar {
  display: contents;
}
.category-directory .category-controls,
.tag-directory .tag-cloud-controls,
.archive-page .archive-view-controls,
.bookmark-directory .bookmark-controls,
.tag-detail-page .tag-detail-controls {
  position: static;
  grid-column: 2;
  grid-row: 1;
  align-self: start;
  margin: 1.25rem 1.875rem 0 .75rem;
  display: flex;
  gap: .1rem;
  padding: .15rem;
  border: 0;
  border-radius: .6rem;
  background: var(--grey-3);
  button {
    box-sizing: border-box;
    display: grid;
    place-items: center;
    width: 2rem;
    height: 1.65rem;
    padding: .25rem .4rem;
    border: 0;
    border-radius: .45rem;
    font-size: 1rem;
    line-height: 1;
    svg { width: 1rem; height: 1rem; }
    &[aria-pressed='true'] { box-shadow: 0 1px 4px var(--box-bg-shadow); }
  }
}
.category-directory .category-cards, .category-directory .category-classic,
.tag-directory .tag.cloud {
  grid-column: 1 / -1;
  min-width: 0;
}
.archive-page .archive-heading-meta {
  grid-column: 1 / -1;
  grid-row: 2;
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: .375rem 1.25rem;
  margin: 0 1.875rem 1.25rem;
}
.archive-page [data-archive-tree-tools] {
  flex-shrink: 0;
  margin-left: auto;
}
@media (max-width: 767px) {
  .category-directory > .item.title, .tag-directory > .item.title { grid-column: 1 / -1; }
  .category-directory .category-controls,
  .tag-directory .tag-cloud-controls {
    grid-row: 2;
    margin: .5rem 0 1rem .75rem;
  }
  .archive-page .archive-heading .item.header { padding-right: 0; }
  .archive-page .archive-view-controls { margin-right: 0; }
  .archive-page .archive-heading-meta { margin-right: 0; }
}
