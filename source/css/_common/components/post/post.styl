.post.block {
  padding: 0 2rem;
}

.md {
  font-family: $font-family-posts;
  word-wrap();

  // Native fragment navigation and scripted restoration share the same offset.
  :is(h1, h2, h3, h4, h5, h6)[id] {
    scroll-margin-top: 4rem;
  }

  .exturl .ic {
    font-size: $font-size-small;
    margin-left: .25rem;
  }

  .button {
    margin-top: 2.5rem;
    text-align: center;
  }
}


@import 'breadcrumb';
@import 'header';
@import 'nav';
@import 'expand';
@import 'rtl';
@import 'footer';
@import 'tags';
@import 'reward';
@import 'copyright';
@import 'historytimeline';
