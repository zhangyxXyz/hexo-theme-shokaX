.select-picker-enhanced > select[hidden], .select-picker-trigger [hidden] { display: none !important; }
.select-picker-trigger {
  display: inline-flex;
  align-items: center;
  gap: .65rem;
  min-width: 1.2rem;
  padding: .15rem .25rem;
  border: 0;
  border-radius: .5rem;
  background: transparent;
  color: inherit;
  font: inherit;
  cursor: pointer;
  span { overflow-wrap: anywhere; }
  &:focus { outline: none; }
  &:focus-visible { box-shadow: 0 0 0 2px var(--color-blue); }
  &[aria-expanded='true'] .select-picker-arrow { transform: rotate(90deg); }
}
.select-picker-arrow {
  display: block;
  width: .75rem;
  height: .75rem;
  flex: 0 0 .75rem;
  transform: rotate(0deg);
  transform-origin: 50% 50%;
  line-height: .75rem;
  font-size: .75rem;
  svg { display: block; width: 100%; height: 100%; }
  opacity: .65;
  transition: transform .18s;
}
.select-picker-menu {
  position: fixed;
  z-index: 1100;
  box-sizing: border-box;
  padding: .25rem;
  max-width: calc(100vw - 24px);
  overflow-y: auto;
  border: 1px solid rgba(120, 179, 199, .12);
  border-radius: .6rem;
  background: var(--grey-1);
  background: unquote('color-mix(in srgb, var(--grey-1) 92%, transparent)');
  backdrop-filter: blur(18px);
  box-shadow: 0 .4rem 1.2rem rgba(0, 0, 0, .13);
  font-size: .75rem;
  line-height: 1.5;
}
.select-picker-option {
  display: flex;
  align-items: center;
  gap: .5rem;
  box-sizing: border-box;
  width: 100%;
  padding: .4rem .55rem;
  border: 0;
  border-radius: .35rem;
  background: transparent;
  color: var(--grey-7);
  text-align: start;
  font: inherit;
  cursor: pointer;
  &:disabled { opacity: .45; cursor: default; }
  .ic { color: var(--grey-6); flex-shrink: 0; font-size: .9rem; }
  span { overflow-wrap: anywhere; }
  .select-picker-check { margin-inline-start: auto; width: .25rem; height: .25rem; flex-shrink: 0; border-radius: 50%; }
  &:hover, &:focus-visible { outline: none; background: rgba(105, 184, 212, .09); }
  &[aria-selected='true'] {
    background: rgba(105, 184, 212, .045);
    color: var(--color-blue);
    .ic { color: inherit; }
    .select-picker-check { background: currentColor; }
  }
}
.select-picker-group + .select-picker-group {
  margin-top: .2rem;
  padding-top: .2rem;
  border-top: 1px solid var(--grey-3);
}
.select-picker-group-label {
  padding: .4rem .55rem .2rem;
  color: var(--grey-5);
  font-size: .7rem;
  font-weight: 600;
}
@media (prefers-reduced-motion: reduce) {
  .select-picker-arrow { transition: none; }
}
