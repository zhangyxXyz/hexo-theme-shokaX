// Global player: it also appears on the homepage, where post.css is absent.
.music-loading-panel {
  position: fixed;
  z-index: $zindex-3;
  left: 3.5rem;
  bottom: 1rem;
  width: calc(100vw - 4.5rem);
  max-width: 25rem;
  padding: 2rem 1rem 1rem;
  border-radius: .8rem;
  background: var(--grey-1-a7);
  backdrop-filter: blur(12px);
  color: var(--text-color);
  box-shadow: 0 .5rem 2rem rgba(0, 0, 0, .1);
  &[hidden] { display: none; }
  .music-loading-close { position: absolute; top: .25rem; right: .75rem; background: transparent; border: 0; color: inherit; font-size: 1.5rem; cursor: pointer; }
  .music-loading-rows { display: grid; gap: .65rem; }
  .music-loading-rows span { height: 1.35rem; background: var(--grey-3); border-radius: .4rem; }
}
#MusicPlayerRoot {
  z-index: $zindex-3;
  --player-background: var(--grey-1-a7) !important;
  --primary-text: var(--text-color) !important;
  --secondary-text: var(--grey-5) !important;
  --player-border: var(--grey-3) !important;
  --playlist-line: var(--grey-3) !important;
  --close-btn: var(--grey-5) !important;
  --hover-btn: var(--color-pink) !important;
  --primary-color-a: var(--color-pink-a3) !important;
  width: calc(100vw - 4.5rem);
  max-width: 25rem;
  min-width: 0;
  max-height: calc(100dvh - 2rem);
  overflow-y: auto;
  img:not([src]) { visibility: hidden; }
  .music-row-loading { cursor: default; color: var(--grey-5); }
  // Placeholders reserve their position even before metadata arrives.
  // Hide their number without removing them from the list's counter.
  li[data-music-status]::before { content: '·'; counter-increment: counter; }
  li[data-music-status="error"] { color: var(--color-pink); opacity: 1; text-decoration: none; }
  if (hexo-config('sidebar.position') == 'right') {
    left: 3.5rem;
    right: auto;
  }
  .music-status {
    margin: .5rem 1rem 0;
    color: var(--color-pink);
    font-size: .8125rem;
  }
  li[role="button"], li[role="slider"] {
    touch-action: manipulation;
    user-select: none;
    &:focus-visible { outline: none; }
  }
  li[role="slider"] {
    touch-action: pan-y;
    cursor: ew-resize;
    &:focus-visible { outline: 2px solid var(--color-pink); outline-offset: -2px; border-radius: .8125rem; }
    &.music-seek-hover::after {
      content: '';
      position: absolute;
      z-index: 2;
      inset-block: .2rem;
      left: clamp(1px, var(--music-seek-target), calc(100% - 2px));
      width: 2px;
      border-radius: 1px;
      background: var(--color-pink);
      pointer-events: none;
    }
    &.music-seeking .progress .bar { width: var(--music-seek-fill) !important; transition: none; }
  }
  .controller {
    display: grid;
    grid-template-columns: repeat(7, minmax(0, 1fr));
    align-items: center;
    justify-items: center;
    padding-inline: .5rem;
    > div, > button {
      width: 100%;
      min-width: 0;
      height: 2.5rem;
      margin: 0;
      font-size: 1.25rem;
      line-height: 1;
    }
    // Nyx icons mask the whole element; size the glyph separately from its hit area.
    [data-music-action] {
      -webkit-mask-size: 1.25rem 1.25rem;
      mask-size: 1.25rem 1.25rem;
      -webkit-mask-position: center;
      mask-position: center;
      -webkit-mask-repeat: no-repeat;
      mask-repeat: no-repeat;
    }
    [data-music-action="toggle"] {
      -webkit-mask-size: 2.125rem 2.125rem;
      mask-size: 2.125rem 2.125rem;
    }
  }
  .music-lyrics-toggle, .music-visualizer-toggle {
    display: flex;
    align-items: center;
    justify-content: center;
    .ic { width: 1.25rem; height: 1.25rem; line-height: 1; }
    padding: 0;
    border: 0;
    border-radius: .5rem;
    background: transparent;
    color: var(--grey-5);
    font: inherit;
    font-size: 1.25rem;
    cursor: pointer;
    &[aria-pressed="true"] { color: var(--color-pink); }
    &:hover { color: var(--color-pink); }
    &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 2px; }
  }
  .music-volume {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 100%;
    height: 2.5rem;
    button {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      height: 100%;
      padding: 0;
      border: 0;
      background: transparent;
      // Nyx highlights the parent div on hover; keep sibling hit areas independent.
      color: var(--primary-text);
      cursor: pointer;
      &:hover { color: var(--color-pink); }
      &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 1px; }
      span { display: block; width: 1.25rem; height: 1.25rem; }
    }
    input {
      --music-volume-color: var(--grey-5);
      --music-volume-thumb-size: 6px;
      position: absolute;
      bottom: -.25rem;
      left: 50%;
      transform: translateX(-50%);
      appearance: none;
      width: 2rem;
      max-width: 80%;
      height: 1rem;
      margin: 0;
      padding: 0;
      border: 0;
      border-radius: 0;
      background: transparent;
      cursor: pointer;
      &:hover, &:active, &:focus-visible { --music-volume-color: var(--color-pink); }
      &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 1px; }
      &::-webkit-slider-runnable-track {
        height: 2px;
        border-radius: 2px;
        background: unquote('linear-gradient(to right, var(--music-volume-color) var(--volume), var(--grey-3) var(--volume))');
      }
      &::-moz-range-track { height: 2px; border-radius: 2px; background: var(--grey-3); }
      &::-moz-range-progress { height: 2px; background: var(--music-volume-color); }
      &::-webkit-slider-thumb {
        appearance: none;
        width: var(--music-volume-thumb-size);
        height: var(--music-volume-thumb-size);
        margin-top: -2px;
        border: 0;
        border-radius: 50%;
        background: var(--music-volume-color);
      }
      &::-moz-range-thumb { width: var(--music-volume-thumb-size); height: var(--music-volume-thumb-size); border: 0; border-radius: 50%; background: var(--music-volume-color); }
    }
  }
}

#music-lyrics-overlay {
  position: fixed;
  z-index: $zindex-3;
  left: 50%;
  bottom: calc(.5rem + env(safe-area-inset-bottom, 0px));
  display: flex;
  align-items: center;
  gap: .375rem;
  width: max-content;
  max-width: calc(100vw - 2rem);
  margin: 0;
  box-sizing: border-box;
  padding: .1875rem .5rem;
  border-radius: 1.5rem;
  background: unquote('color-mix(in srgb, var(--grey-1) 78%, transparent)');
  border: 1px solid unquote('color-mix(in srgb, var(--text-color) 10%, transparent)');
  box-shadow: 0 .5rem 1.75rem rgba(0, 0, 0, .14);
  -webkit-backdrop-filter: unquote('blur(22px) saturate(125%)');
  backdrop-filter: unquote('blur(22px) saturate(125%)');
  color: var(--color-pink);
  font-size: .9375rem;
  font-weight: 400;
  line-height: 1;
  text-align: center;
  cursor: default;
  pointer-events: none;
  visibility: hidden;
  opacity: 0;
  transform: translate(-50%, .5rem);
  transition: opacity .2s ease, transform .2s ease, visibility .2s;
  .music-lyrics-controls {
    flex: 0 0 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0;
  }
  .music-lyrics-close {
    flex: 0 0 1.375rem;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.375rem;
    padding: 0;
    border: 0;
    background: transparent;
    color: inherit;
    font-size: 1.125rem;
    cursor: pointer;
    &:hover { color: var(--primary-color); }
    &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 1px; }
  }
  .music-lyrics-disc {
    position: relative;
    flex: 0 0 3rem;
    width: 3rem;
    height: 3rem;
    border-radius: 50%;
    background: repeating-radial-gradient(circle, #171719 0px, #171719 2px, #242427 3px, #171719 4px);
    box-shadow: 0 .25rem .65rem rgba(0, 0, 0, .2);
    animation: music-vinyl-turn 18s linear infinite;
    animation-play-state: paused;
    img { position: absolute; width: 60%; height: 60%; left: 20%; top: 20%; border-radius: 50%; object-fit: cover; }
    img[hidden] { display: none; }
    &::after { content: ''; position: absolute; left: calc(50% - 2px); top: calc(50% - 2px); width: 4px; height: 4px; border-radius: 50%; background: #18181a; }
  }
  &.is-visible.is-playing:not(.is-document-hidden) .music-lyrics-disc { animation-play-state: running; }
  .music-lyrics-next {
    flex: 0 0 1.375rem;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.375rem;
    padding: 0;
    border: 0;
    background: transparent;
    color: inherit;
    font-size: 1.125rem;
    cursor: pointer;
    transition: color .15s ease;
    &:hover { color: var(--primary-color); }
    &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 3px; }
    &:disabled { opacity: .5; cursor: default; }
  }
  .music-lyrics-lines {
    box-sizing: border-box;
    flex: 0 1 auto;
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: repeat(2, 1.375rem);
    padding: .125rem .5rem;
    width: max-content;
    min-width: unquote('min(14rem, calc(100vw - 10rem))');
    max-width: unquote('min(32rem, calc(100vw - 10rem))');
    overflow: hidden;
    &.is-single {
      .music-lyric-line:not(.is-leaving) { grid-row: 1 / 3; height: 2.75rem; justify-content: center; }
      .music-lyric-line.is-next { display: none; }
    }
  }
  .music-lyric-line {
    display: flex;
    align-items: center;
    justify-content: flex-start;
    min-width: 0;
    height: 1.375rem;
    transform-origin: left center;
    text-shadow: 0 1px 2px unquote('color-mix(in srgb, var(--color-pink) 18%, transparent)'), 0 0 .45rem unquote('color-mix(in srgb, var(--color-pink) 12%, transparent)');
    span {
      display: block;
      min-width: 0;
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
      line-height: 1.2;
    }
    &.is-next {
      text-shadow: none;
      justify-content: flex-end;
      font-size: .875rem;
      color: unquote('color-mix(in srgb, var(--text-color) 80%, var(--grey-1))');
    }
    &.is-leaving {
      position: absolute;
      top: .125rem;
      left: .5rem;
      width: calc(100% - 1rem);
    }
  }
  &.is-visible {
    pointer-events: auto;
    opacity: 1;
    visibility: visible;
    transform: translate(-50%, 0);
  }
  +tablet-mobile() {
    bottom: calc(3.5rem + env(safe-area-inset-bottom, 0px));
    gap: .375rem;
    .music-lyrics-disc { flex-basis: 3rem; width: 3rem; height: 3rem; }
    .music-lyrics-lines { padding-inline: .75rem; }
    .music-lyric-line { font-size: .875rem; }
    .music-lyric-line.is-next { font-size: .8125rem; }
    .music-lyric-line.is-leaving { left: .75rem; width: calc(100% - 1.5rem); }
  }
  @media (prefers-reduced-motion: reduce) {
    transition: none;
    transform: translateX(-50%);
    &.is-visible { transform: translateX(-50%); }
    .music-lyrics-disc { animation: none; }
  }
}

@keyframes music-vinyl-turn {
  to { transform: rotate(360deg); }
}

#tool {
  .reading-tool { border: 0; padding: 0; background: transparent; font: inherit; }
  .reading-tool[hidden] { display: none !important; }
  .reading-tool:disabled { cursor: wait; }
  position: fixed;
  if (hexo-config('sidebar.position') == 'left') {
    right: 1rem;
  } else {
    left: 1rem;
  }

  top: 50vh;
  // Keep controls above the Live2D canvas (10), including at narrow widths.
  z-index: $zindex-2 + 2;


  +tablet-mobile() {
    right: 1rem;
    left: auto;
  }

  .item {
    border-radius: .5rem;
    &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 2px; }
    &:focus:not(:focus-visible) { outline: none; }
    display: none;
    width: 1.875rem;
    height: 1.875rem;
    opacity: .5;
    cursor: pointer;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    the-transition();
    &:hover {
      opacity: .9;
    }
  }

  .player {
    display: inline-flex;
    flex-direction: row-reverse;
    font-size: $font-size-largest;
    width: auto;
    height: auto;

    > .btn {
      border: 0;
      padding: 0;
      background: transparent;
      color: inherit;
      font-size: inherit;
      cursor: pointer;
      font-family-icons();
      width: 1.875rem;
      min-height: 1.875rem;
      text-align: center;
      &:disabled { cursor: progress; opacity: .45; }
      &:focus-visible { outline: 2px solid var(--primary-color); outline-offset: 2px; }
      &+.btn {
        margin-right: .625rem;
      }
    }

    .play-pause {
      &[data-play="true"]::before { @extend .i-pause:before; }
      &::before {
        @extend .i-play:before;
      }
    }

    .music {
      display: block;
      &::before {
        @extend .i-music:before;
      }
    }
  }

  &.playing .player {
    .play-pause::before {
      @extend .i-pause:before;
    }
  }

  .player-info {
    shadow-box();
    background: var(--grey-1-a7);
    backdrop-filter: blur(.625rem);
    position: fixed;
    display: none;
    bottom: 0;
    overflow: hidden;

    if (hexo-config('sidebar.position') == 'left') {
      right: 3.5rem;
    } else {
      left: 3.5rem;
    }
    width: 50vw;
    z-index: $zindex-2;
    max-width: 25rem;

    +mobile() {
      right: 2.5rem;
    }

    .nav {
      font-size: $font-size-smallest;
      height: 2.1875rem;
    }
  }

  .back-to-top {
    span {
      font-size: $font-size-smallest;
    }
  }


  &.affix {
    text-shadow: none;
    top: auto;
    box-shadow: 0 0 .5rem rgba(0,0,0,.1);
    background: var(--grey-1-a3);
    border-radius: .3125rem;
    if (hexo-config('sidebar.position') == 'left') {
      right: 0;
    } else {
      left: 0;
    }
    bottom: 0;

    .item {
      display: flex;
      color: var(--primary-color);
    }

    .player {
      font-size: $font-size-base;
      flex-direction: column-reverse;

      > .btn+.btn {
        margin-right: 0;
      }

      .music {
        display: block;
      }

    }

    .player-info {
      if (hexo-config('sidebar.position') == 'left') {
        right: 2.5rem;
      } else {
        left: 2.5rem;
      }

      +desktop-large() {
        if (hexo-config('sidebar.position') == 'left') {
          right: 3.5rem;
        } else {
          left: 3.5rem;
        }
      }

      +tablet-mobile() {
        right: 2.5rem;
        left: auto;
      }

      &.show {
        display: block;
        if (hexo-config('sidebar.position') == 'left') {
          @extend .slide-right-in;
        } else {
          @extend .slide-left-in;

          +tablet-mobile() {
            animation: slideRightIn .3s;
          }
        }

        &.hide {
          @extend .fade-out;
        }
      }
    }

    .back-to-top {
      align-items: center;
      justify-content: center;
      height: auto;
      padding: .3125rem 0 0;
    }

    .contents {
      display: none;

      +tablet-mobile() {
        display: flex;
      }
    }

    +desktop-large() {
      if (hexo-config('sidebar.position') == 'left') {
        right: 1rem;
      } else {
        left: 1rem;
      }
      bottom: 1rem;
    }

    +tablet-mobile() {
      right: 0;
      bottom: 0;
      left: auto;
    }
  }
}
